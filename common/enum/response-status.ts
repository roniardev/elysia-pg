export enum ResponseErrorStatus {
	UNAUTHORIZED = 401,
	FORBIDDEN = 403,
	NOT_FOUND = 404,
	INTERNAL_SERVER_ERROR = 500,
}

export enum ResponseSuccessStatus {
	OK = 200,
	CREATED = 201,
	ACCEPTED = 202,
}
